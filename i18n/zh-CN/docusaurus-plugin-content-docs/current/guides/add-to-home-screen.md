---
title: 添加到主屏幕
---

# 添加到主屏幕

长按应用图标，可以看到两种把应用添加到主屏幕的方法：

1. **启动 URL**：复制 LiveContainer 提供的 URL，创建一个用于打开该 URL 的快捷指令，再把快捷指令添加到主屏幕。每个应用都需要单独创建一个快捷指令。
2. **创建 App Clip**：安装 App Clip 的 MDM 描述文件，系统会把应用添加到主屏幕。需要注意：启动时会多一次跳转，并且 App Clip 会一直留在应用切换器中。
