---
title: 多个 LiveContainer
---

# 多个 LiveContainer

安装多个 LiveContainer 并将应用转换为“共享应用”后，你可以在这些容器之间共享同一个应用，并保持其大部分数据与设置。

## 安装额外的 LiveContainer

要启用多实例：

1. 打开设置，点击「安装另一个 LiveContainer」
2. 默认启动的是首个容器（蓝色图标）
3. 想使用第二个容器（灰色图标）时：
   - 在第一个容器中将应用转换为“共享应用”
   - 然后从第二个容器启动该应用
